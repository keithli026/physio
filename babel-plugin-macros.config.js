module.exports = {
    // ...
    // Other macros config
    styledComponents: {
      pure: true,
      // Debugging Usage, by default displayName, filename are set to be true, e.g. filename__displayName prefix
      // displayName: false,
      // fileName: false,
      meaninglessFileNames: ['index', 'style'],
    },
}